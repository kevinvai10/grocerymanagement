import React from 'react'

class CartContainer extends React.Component {
    constructor(props){
        super(props);
        this.state={
            cartItems: this.props.cart
        }
    }

    <table>
        <tbody>
            //iterate over the elements and display cartitem
        </tbody>
    </table>
}